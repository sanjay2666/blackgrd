<!DOCTYPE html><html lang="en"><head>@include('admin.common.head')</head><body class="hold-transition sidebar-mini"><div class="wrapper">@include('admin.common.header') @include('admin.common.sidebar')<div class="content-wrapper"><section class="content-header"><div class="header-icon"><i class="fa fa-cogs"></i></div><div class="header-title"><h1>Machines</h1><small>Machine Master</small></div></section><section class="content">{!! display_message('message') !!}<div class="panel panel-bd lobidrag"><div class="panel-heading"><h4>Machine List</h4></div><div class="panel-body"><form action="{{ route('admin.machines.index') }}" method="GET" class="row" style="margin-bottom:10px"><div class="col-sm-4"><input name="search" value="{{ request('search') }}" class="form-control" placeholder="Search machine"></div><div class="col-sm-3"><select name="process_id" class="form-control"><option value="">All Processes</option>@foreach($processItems as $process)<option value="{{ $process->id }}" @selected(request('process_id') == $process->id)>{{ $process->process_name }}</option>@endforeach</select></div><div class="col-sm-2"><select name="status" class="form-control"><option value="">All Statuses</option><option value="Active" @selected(request('status') === 'Active')>Active</option><option value="Inactive" @selected(request('status') === 'Inactive')>Inactive</option></select></div><div class="col-sm-1"><button class="btn btn-add">Search</button></div><div class="col-sm-2"><a href="{{ route('admin.machines.create') }}" class="btn btn-add"><i class="fa fa-plus"></i> Add Machine</a></div></form><div class="table-responsive"><table class="table table-bordered table-striped"><thead><tr class="info"><th>Name</th><th>Process</th><th>Department</th><th>Factory</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($machines as $machine)<tr><td>{{ $machine->name }}</td><td>{{ $machine->processItem?->process_name ?? '-' }}</td><td>{{ $machine->department?->department_name ?? '-' }}</td><td>{{ $machine->factory?->name ?? '-' }}</td><td>{{ $machine->status }}</td><td><a href="{{ route('admin.machines.edit', enc($machine->id)) }}" class="btn btn-xs btn-default"><i class="fa fa-pencil"></i></a> <form method="POST" action="{{ route($machine->status === 'Active' ? 'admin.machines.deactivate' : 'admin.machines.activate', $machine) }}" style="display:inline">@csrf @method('PATCH')<button class="btn btn-xs {{ $machine->status === 'Active' ? 'btn-warning' : 'btn-success' }}">{{ $machine->status === 'Active' ? 'Deactivate' : 'Activate' }}</button></form></td></tr>@empty<tr><td colspan="6" class="text-center">No records found.</td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $machines->links() }}</div></div></div></section></div>@include('admin.common.footer') @include('admin.common.formfooterscript')</div></body></html>
