<!DOCTYPE html><html lang="en"><head>@include('admin.common.head')</head><body class="hold-transition sidebar-mini"><div class="wrapper">@include('admin.common.header') @include('admin.common.sidebar')<div class="content-wrapper"><section class="content-header"><div class="header-icon"><i class="fa fa-tint"></i></div><div class="header-title"><h1>Coating Type Master</h1><small>Reusable coating finish/type identity</small></div></section><section class="content">{!! display_message('message') !!}<div class="panel panel-bd lobidrag"><div class="panel-heading"><a href="{{ route('admin.cotings.create') }}" class="btn btn-add"><i class="fa fa-plus"></i> Add Coating Type</a></div><div class="panel-body"><form method="GET" class="form-inline"><input name="search" value="{{ request('search') }}" class="form-control input-sm" placeholder="Search name or code"><select name="status" class="form-control input-sm"><option value="">All statuses</option>@foreach($statusOptions as $value => $label)<option value="{{ $value }}" @selected(request('status') === $value)>{{ $label }}</option>@endforeach</select><button class="btn btn-primary btn-sm">Search</button> <a href="{{ route('admin.cotings.index') }}" class="btn btn-default btn-sm">Reset</a></form><div class="table-responsive"><table class="table table-bordered table-striped"><thead><tr><th>Name</th><th>Code</th><th>Description</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($cotings as $coting)<tr><td>{{ $coting->name }}</td><td>{{ $coting->code ?: '—' }}</td><td>{{ \Illuminate\Support\Str::limit($coting->description, 100) ?: '—' }}</td><td>{{ $coting->status }}</td><td><a class="btn btn-xs btn-info" href="{{ route('admin.cotings.edit', enc($coting->id)) }}">Edit</a> @if($coting->status === 'Active')<form class="inline-form" method="POST" action="{{ route('admin.cotings.deactivate', enc($coting->id)) }}">@csrf @method('PATCH')<button class="btn btn-xs btn-warning">Deactivate</button></form>@else<form class="inline-form" method="POST" action="{{ route('admin.cotings.activate', enc($coting->id)) }}">@csrf @method('PATCH')<button class="btn btn-xs btn-success">Activate</button></form>@endif</td></tr>@empty<tr><td colspan="5">No Coating Types found.</td></tr>@endforelse</tbody></table></div>{{ $cotings->links('vendor.pagination.bootstrap-4') }}</div></div></section></div>@include('admin.common.footer')</div>@include('admin.common.formfooterscript')</body></html>
