<!DOCTYPE html><html lang="en"><head>@include('admin.common.head')</head><body class="hold-transition sidebar-mini"><div class="wrapper">@include('admin.common.header') @include('admin.common.sidebar')<div class="content-wrapper"><section class="content-header"><div class="header-icon"><i class="fa fa-building"></i></div><div class="header-title"><h1>Customers</h1><small>Customer Master</small></div></section><section class="content">{!! display_message('message') !!}<div class="panel panel-bd"><div class="panel-heading"><h4>Customer List</h4></div><div class="panel-body"><form action="{{ route('admin.customers.index') }}" method="GET" class="row" style="margin-bottom:10px"><div class="col-sm-5"><input name="search" value="{{ request('search') }}" class="form-control" placeholder="Search name, code, GSTIN or mobile"></div><div class="col-sm-2"><select name="status" class="form-control"><option value="">All Statuses</option><option value="Active" @selected(request('status') === 'Active')>Active</option><option value="Inactive" @selected(request('status') === 'Inactive')>Inactive</option></select></div><div class="col-sm-2"><button class="btn btn-add">Search</button></div><div class="col-sm-3 text-right"><a href="{{ route('admin.customers.create') }}" class="btn btn-add"><i class="fa fa-plus"></i> Add Customer</a></div></form><div class="table-responsive"><table class="table table-bordered table-striped"><thead><tr class="info"><th>Name</th><th>Code</th><th>GSTIN</th><th>Contact</th><th>Addresses</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($customers as $customer)<tr><td>{{ $customer->name }}</td><td>{{ $customer->customer_code ?: '-' }}</td><td>{{ $customer->gstin ?: '-' }}</td><td>{{ $customer->phone ?: ($customer->email ?: '-') }}</td><td>{{ $customer->activeAddresses->count() }}</td><td>{{ $customer->status }}</td><td><a href="{{ route('admin.customers.edit', $customer) }}" class="btn btn-xs btn-default"><i class="fa fa-pencil"></i></a> <form method="POST" action="{{ route($customer->status === 'Active' ? 'admin.customers.deactivate' : 'admin.customers.activate', $customer) }}" style="display:inline">@csrf @method('PATCH')<button class="btn btn-xs {{ $customer->status === 'Active' ? 'btn-warning' : 'btn-success' }}">{{ $customer->status === 'Active' ? 'Deactivate' : 'Activate' }}</button></form> <form method="POST" action="{{ route('admin.customers.destroy', $customer) }}" style="display:inline">@csrf @method('DELETE')<button class="btn btn-xs btn-danger">Remove</button></form></td></tr>@empty<tr><td colspan="7" class="text-center">No records found.</td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $customers->links() }}</div></div></div></section></div>@include('admin.common.footer') @include('admin.common.formfooterscript')</div></body></html>
