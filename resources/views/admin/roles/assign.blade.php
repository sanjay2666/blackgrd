<!DOCTYPE html><html><head>@include('admin.common.head')</head><body class="hold-transition sidebar-mini"><div class="wrapper">@include('admin.common.header')@include('admin.common.sidebar')<div class="content-wrapper"><section class="content"><h3>Assign {{ $role->name }}</h3><form method="POST" action="{{ route('admin.roles.assign.store', $role) }}">@csrf<select class="form-control" name="user_id" required><option value="">Select eligible company user</option>@foreach($users as $user)<option value="{{ $user->id }}">{{ $user->name }} ({{ $user->email }})</option>@endforeach</select><button class="btn btn-primary" type="submit">Assign</button></form></section></div></div>@include('admin.common.footer')</body></html>
