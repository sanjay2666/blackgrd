<!DOCTYPE html><html><head>@include('admin.common.head')</head><body class="hold-transition sidebar-mini"><div class="wrapper">@include('admin.common.header')@include('admin.common.sidebar')<div class="content-wrapper"><section class="content"><h3>ERP roles</h3><p>Company Admin roles for this single company. Super Admin is a reserved system role and is never assignable here.</p><a class="btn btn-primary" href="{{ route('admin.roles.create') }}">Create role</a><table class="table table-striped"><thead><tr><th>Name</th><th>Status</th><th>Permissions</th><th></th></tr></thead><tbody>@foreach($roles as $role)<tr><td>{{ $role->name }}</td><td>{{ $role->status }}</td><td>{{ $role->permissions->count() }}</td><td><a href="{{ route('admin.roles.edit', $role) }}">Edit</a> | <a href="{{ route('admin.roles.assign', $role) }}">Assign users</a></td></tr>@endforeach</tbody></table></section></div></div>@include('admin.common.footer')</body></html>
