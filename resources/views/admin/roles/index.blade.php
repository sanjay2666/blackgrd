<!DOCTYPE html><html><head>@include('admin.common.head')</head><body class="hold-transition sidebar-mini"><div class="wrapper">@include('admin.common.header')@include('admin.common.sidebar')<div class="content-wrapper"><section class="content"><h3>ERP roles</h3><p>Company roles for this single company. The reserved Super Admin role is never shown or assignable here.</p><p><a class="btn btn-primary" href="{{ route('admin.roles.create') }}">Create role</a></p><input id="role-filter" class="form-control" type="search" placeholder="Search roles..." aria-label="Search roles"><table class="table table-striped" id="roles-table"><thead><tr><th>Name</th><th>Panel</th><th>Status</th><th>Permissions</th><th></th></tr></thead><tbody>@forelse($roles as $role)<tr><td>{{ $role->name }}<br><small>{{ $role->description }}</small></td><td><span class="label label-{{ $role->panel === 'Admin' ? 'primary' : 'info' }}">{{ $role->panel }}</span></td><td>{{ $role->status }}</td><td>{{ $role->permissions->count() }}</td><td><a href="{{ route('admin.roles.edit', $role) }}">Edit permissions</a> | <a href="{{ route('admin.roles.assign', $role) }}">Assign users</a></td></tr>@empty<tr><td colspan="5">No company roles found.</td></tr>@endforelse</tbody></table></section></div></div>@include('admin.common.footer')<script>jQuery(function($){$('#role-filter').on('input',function(){var q=this.value.toLowerCase();$('#roles-table tbody tr').each(function(){$(this).toggle($(this).text().toLowerCase().indexOf(q)!==-1);});});});</script></body></html>
