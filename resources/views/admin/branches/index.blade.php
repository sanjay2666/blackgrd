<!DOCTYPE html><html lang="en"><head>@include('admin.common.head')</head><body class="hold-transition sidebar-mini"><div class="wrapper">@include('admin.common.header') @include('admin.common.sidebar')<div class="content-wrapper"><section class="content-header"><div class="header-icon"><i class="fa fa-building"></i></div><div class="header-title"><h1>Branches / Factories</h1><small>Operational locations of the canonical company</small></div></section><section class="content">{!! display_message('message') !!}<div class="panel panel-bd lobidrag"><div class="panel-heading"><form class="form-inline"><input name="search" value="{{ request('search') }}" class="form-control" placeholder="Search name"><select name="status" class="form-control"><option value="">All statuses</option><option value="Active" @selected(request('status') === 'Active')>Active</option><option value="Inactive" @selected(request('status') === 'Inactive')>Inactive</option></select><button class="btn btn-default">Filter</button><a href="{{ route('admin.branches.create') }}" class="btn btn-success pull-right">Add Branch</a><a href="{{ route('admin.factories.create') }}" class="btn btn-primary pull-right" style="margin-right:6px">Add Factory</a></form></div><div class="panel-body"><h4>Branches</h4><div class="table-responsive"><table class="table table-bordered table-striped"><tr><th>Name</th><th>Code</th><th>Kind</th><th>City</th><th>GSTIN</th><th>Status</th><th>Actions</th></tr>@forelse($branches as $row)<tr><td>{{ $row->name }}</td><td>{{ $row->branch_code }}</td><td>{{ ucwords(str_replace('_',' ',$row->kind)) }}</td><td>{{ $row->city ?: '—' }}</td><td>{{ $row->gstin ?: 'Company GSTIN' }}</td><td>{{ $row->status }}</td><td><a href="{{ route('admin.branches.edit',$row) }}" class="btn btn-xs btn-default">Edit</a>@if($row->status === 'Active')<form method="POST" action="{{ route('admin.branches.deactivate',$row) }}" class="inline-form">@csrf @method('PATCH')<button class="btn btn-xs btn-warning">Deactivate</button></form>@else<form method="POST" action="{{ route('admin.branches.activate',$row) }}" class="inline-form">@csrf @method('PATCH')<button class="btn btn-xs btn-success">Activate</button></form>@endif</td></tr>@empty<tr><td colspan="7">No branches found.</td></tr>@endforelse</table></div>{{ $branches->links() }}<hr><h4>Factories</h4><div class="table-responsive"><table class="table table-bordered table-striped"><tr><th>Name</th><th>Code</th><th>Branch</th><th>City</th><th>GSTIN</th><th>Status</th><th>Actions</th></tr>@forelse($factories as $row)<tr><td>{{ $row->name }}</td><td>{{ $row->factory_code }}</td><td>{{ $row->branch?->name ?: '—' }}</td><td>{{ $row->city ?: '—' }}</td><td>{{ $row->gstin ?: 'Company GSTIN' }}</td><td>{{ $row->status }}</td><td><a href="{{ route('admin.factories.edit',$row) }}" class="btn btn-xs btn-default">Edit</a>@if($row->status === 'Active')<form method="POST" action="{{ route('admin.factories.deactivate',$row) }}" class="inline-form">@csrf @method('PATCH')<button class="btn btn-xs btn-warning">Deactivate</button></form>@else<form method="POST" action="{{ route('admin.factories.activate',$row) }}" class="inline-form">@csrf @method('PATCH')<button class="btn btn-xs btn-success">Activate</button></form>@endif</td></tr>@empty<tr><td colspan="7">No factories found.</td></tr>@endforelse</table></div>{{ $factories->links() }}</div></div></section></div>@include('admin.common.footer')</div>@include('admin.common.formfooterscript')</body></html>
