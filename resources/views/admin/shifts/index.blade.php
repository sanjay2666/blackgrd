<!DOCTYPE html><html lang="en"><head>@include('admin.common.head')</head><body class="hold-transition sidebar-mini"><div class="wrapper">@include('admin.common.header') @include('admin.common.sidebar')<div class="content-wrapper"><section class="content-header"><div class="header-icon"><i class="fa fa-clock-o"></i></div><div class="header-title"><h1>Shifts</h1><small>Shift Master</small></div></section><section class="content">{!! display_message('message') !!}<div class="panel panel-bd"><div class="panel-heading"><h4>Shift List</h4></div><div class="panel-body"><form action="{{ route('admin.shifts.index') }}" method="GET" class="row" style="margin-bottom:10px"><div class="col-sm-4"><input name="search" value="{{ request('search') }}" class="form-control" placeholder="Search name or code"></div><div class="col-sm-3"><select name="factory_id" class="form-control"><option value="">All Scopes</option><option value="">Company Global</option>@foreach($factories as $factory)<option value="{{ $factory->id }}" @selected(request('factory_id') == $factory->id)>{{ $factory->name }}</option>@endforeach</select></div><div class="col-sm-2"><select name="status" class="form-control"><option value="">All Statuses</option><option value="Active" @selected(request('status') === 'Active')>Active</option><option value="Inactive" @selected(request('status') === 'Inactive')>Inactive</option></select></div><div class="col-sm-1"><button class="btn btn-add">Search</button></div><div class="col-sm-2"><a href="{{ route('admin.shifts.create') }}" class="btn btn-add"><i class="fa fa-plus"></i> Add Shift</a></div></form><div class="table-responsive"><table class="table table-bordered table-striped"><thead><tr class="info"><th>Name</th><th>Code</th><th>Start</th><th>End</th><th>Duration</th><th>Scope</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($shifts as $shift)<tr><td>{{ $shift->shift_name }}</td><td>{{ $shift->shift_code ?: '-' }}</td><td>{{ substr($shift->start_time, 0, 5) }}</td><td>{{ substr($shift->end_time, 0, 5) }}</td><td>{{ intdiv($shift->duration_minutes, 60) }}h {{ $shift->duration_minutes % 60 }}m{{ substr($shift->end_time, 0, 5) < substr($shift->start_time, 0, 5) ? ' overnight' : '' }}</td><td>{{ $shift->factory?->name ?? 'Company Global' }}</td><td>{{ $shift->status }}</td><td><a href="{{ route('admin.shifts.edit', $shift) }}" class="btn btn-xs btn-default"><i class="fa fa-pencil"></i></a> <form method="POST" action="{{ route($shift->status === 'Active' ? 'admin.shifts.deactivate' : 'admin.shifts.activate', $shift) }}" style="display:inline">@csrf @method('PATCH')<button class="btn btn-xs {{ $shift->status === 'Active' ? 'btn-warning' : 'btn-success' }}">{{ $shift->status === 'Active' ? 'Deactivate' : 'Activate' }}</button></form> <form method="POST" action="{{ route('admin.shifts.destroy', $shift) }}" style="display:inline">@csrf @method('DELETE')<button class="btn btn-xs btn-danger">Remove</button></form></td></tr>@empty<tr><td colspan="8" class="text-center">No records found.</td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $shifts->links() }}</div></div></div></section></div>@include('admin.common.footer') @include('admin.common.formfooterscript')</div></body></html>
