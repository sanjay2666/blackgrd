<!DOCTYPE html><html lang="en"><head>@include('admin.common.head')</head><body class="hold-transition sidebar-mini"><div class="wrapper">@include('admin.common.header') @include('admin.common.sidebar')<div class="content-wrapper"><section class="content-header"><div class="header-icon"><i class="fa fa-truck"></i></div><div class="header-title"><h1>Transporters</h1><small>Transporter Master</small></div></section><section class="content">{!! display_message('message') !!}<div class="panel panel-bd"><div class="panel-heading"><h4>Transporter List</h4></div><div class="panel-body"><form action="{{ route('admin.transporters.index') }}" method="GET" class="row" style="margin-bottom:10px"><div class="col-sm-5"><input name="search" value="{{ request('search') }}" class="form-control" placeholder="Search name, code, GSTIN or mobile"></div><div class="col-sm-2"><select name="status" class="form-control"><option value="">All Statuses</option><option value="Active" @selected(request('status') === 'Active')>Active</option><option value="Inactive" @selected(request('status') === 'Inactive')>Inactive</option></select></div><div class="col-sm-2"><button class="btn btn-add">Search</button></div><div class="col-sm-3 text-right"><a href="{{ route('admin.transporters.create') }}" class="btn btn-add"><i class="fa fa-plus"></i> Add Transporter</a></div></form><div class="table-responsive"><table class="table table-bordered table-striped"><thead><tr class="info"><th>Name</th><th>Code</th><th>GSTIN</th><th>Contact</th><th>City / State</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($transporters as $transporter)<tr><td>{{ $transporter->name }}</td><td>{{ $transporter->transporter_code ?: '-' }}</td><td>{{ $transporter->gstin ?: '-' }}</td><td>{{ $transporter->phone ?: ($transporter->email ?: '-') }}</td><td>{{ optional($transporter->activeAddresses->first())->city ?: '-' }}</td><td>{{ $transporter->status }}</td><td><a href="{{ route('admin.transporters.edit',$transporter) }}" class="btn btn-xs btn-default">Edit</a> <form method="POST" action="{{ route($transporter->status === 'Active' ? 'admin.transporters.deactivate' : 'admin.transporters.activate',$transporter) }}" style="display:inline">@csrf @method('PATCH')<button class="btn btn-xs {{ $transporter->status === 'Active' ? 'btn-warning' : 'btn-success' }}">{{ $transporter->status === 'Active' ? 'Deactivate' : 'Activate' }}</button></form> <form method="POST" action="{{ route('admin.transporters.destroy',$transporter) }}" style="display:inline">@csrf @method('DELETE')<button class="btn btn-xs btn-danger">Remove</button></form></td></tr>@empty<tr><td colspan="7" class="text-center">No records found.</td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $transporters->links() }}</div></div></div></section></div>@include('admin.common.footer') @include('admin.common.formfooterscript')</div></body></html>
