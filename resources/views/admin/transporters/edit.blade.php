<!DOCTYPE html><html lang="en"><head>@include('admin.common.head')</head><body class="hold-transition sidebar-mini"><div class="wrapper">@include('admin.common.header') @include('admin.common.sidebar')<div class="content-wrapper"><section class="content-header"><h1>Edit Transporter</h1></section><section class="content">{!! display_message('message') !!}<div class="panel panel-bd"><div class="panel-body"><form method="POST" action="{{ route('admin.transporters.update',$transporter) }}">@csrf @method('PUT') @include('admin.transporters.form')<button class="btn btn-success">Save Transporter</button> <a href="{{ route('admin.transporters.index') }}" class="btn btn-warning">Cancel</a></form></div></div><div class="panel panel-bd"><div class="panel-heading"><h4>Billing / Shipping Addresses <a href="{{ route('admin.transporters.addresses.create',$transporter) }}" class="btn btn-xs btn-add pull-right">Add Address</a></h4></div><div class="panel-body"><table class="table table-bordered table-striped"><thead><tr class="info"><th>Type</th><th>Address</th><th>State</th><th>Default</th><th>Actions</th></tr></thead><tbody>@forelse($transporter->activeAddresses as $address)<tr><td>{{ $address->address_type === 'b' ? 'Billing' : 'Shipping' }}</td><td>{{ $address->address_1 }}, {{ $address->city }} - {{ $address->zip_code }}</td><td>{{ $address->state?->name ?? '-' }}</td><td>{{ $address->default_address ? 'Yes' : 'No' }}</td><td><a href="{{ route('admin.transporters.addresses.edit',[$transporter,$address]) }}" class="btn btn-xs btn-default">Edit</a> <form method="POST" action="{{ route('admin.transporters.addresses.destroy',[$transporter,$address]) }}" style="display:inline">@csrf @method('DELETE')<button class="btn btn-xs btn-danger">Remove</button></form></td></tr>@empty<tr><td colspan="5" class="text-center">No active addresses.</td></tr>@endforelse</tbody></table></div></div></section></div>@include('admin.common.footer') @include('admin.common.formfooterscript')</div></body></html>
