<!DOCTYPE html><html lang="en"><head>@include('admin.common.head')</head><body class="hold-transition sidebar-mini"><div class="wrapper">@include('admin.common.header') @include('admin.common.sidebar')<div class="content-wrapper"><section class="content-header"><div class="header-icon"><i class="fa fa-random"></i></div><div class="header-title"><h1>{{ $definition->workflow_name }}</h1><small>{{ $definition->workflow_code }} · Version {{ $version->version_number }} · {{ $version->status }}</small></div></section><section class="content">{!! display_message('message') !!}<div class="panel panel-bd"><div class="panel-heading"><a href="{{ route('admin.workflow-definitions.index') }}" class="btn btn-default">Workflow List</a> @if($definition->status === 'Active')<form method="POST" action="{{ route('admin.workflow-definitions.versions.store', $definition) }}" style="display:inline">@csrf<button class="btn btn-add">Create New Version</button></form>@endif @if($version->status === 'Draft')<form method="POST" action="{{ route('admin.workflow-definitions.versions.finalize', [$definition, $version]) }}" style="display:inline">@csrf @method('PATCH')<button class="btn btn-success">Finalize Version</button></form>@endif</div><div class="panel-body"><form method="POST" action="{{ route('admin.workflow-definitions.update', $definition) }}">@csrf @method('PUT') @include('admin.workflow_definitions.form')<button class="btn btn-primary">Save Definition</button></form><hr><h4>Ordered Process Steps</h4><div class="table-responsive"><table class="table table-bordered"><thead><tr class="info"><th>Sequence</th><th>Process</th><th>Label</th><th>Description</th><th>Action</th></tr></thead><tbody>@forelse($version->steps as $step)<tr><td>{{ $step->sequence }}</td><td>{{ $step->process->process_name ?? $step->process_id }}</td><td>{{ $step->step_label ?: '-' }}</td><td>{{ $step->description ?: '-' }}</td><td>@if($version->status === 'Draft')<form method="POST" action="{{ route('admin.workflow-definitions.steps.destroy', [$definition, $version, $step]) }}">@csrf @method('DELETE')<button class="btn btn-xs btn-danger">Remove</button></form>@else Locked @endif</td></tr>@empty<tr><td colspan="5" class="text-center">No steps yet. Add at least one before finalizing.</td></tr>@endforelse</tbody></table></div>@if($version->status === 'Draft')<hr><h4>Add Process Step</h4><form method="POST" action="{{ route('admin.workflow-definitions.steps.store', [$definition, $version]) }}" class="row">@csrf<div class="col-sm-2"><input type="number" min="1" name="sequence" class="form-control" placeholder="Sequence" required></div><div class="col-sm-4"><select name="process_id" class="form-control" required><option value="">Select Process</option>@foreach($processes as $process)<option value="{{ $process->id }}">{{ $process->process_name }}</option>@endforeach</select></div><div class="col-sm-3"><input name="step_label" class="form-control" placeholder="Step label"></div><div class="col-sm-3"><button class="btn btn-success">Add Step</button></div></form>@endif</div></div></section></div>@include('admin.common.footer') @include('admin.common.formfooterscript')</div></body></html>
