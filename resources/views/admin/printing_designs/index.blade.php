<!DOCTYPE html><html lang="en"><head>@include('admin.common.head')</head><body class="hold-transition sidebar-mini"><div class="wrapper">@include('admin.common.header') @include('admin.common.sidebar')<div class="content-wrapper"><section class="content-header"><div class="header-icon"><i class="fa fa-picture-o"></i></div><div class="header-title"><h1>Printing Design Master</h1><small>Reusable artwork/design identity</small></div></section><section class="content">{!! display_message('message') !!}<div class="panel panel-bd lobidrag"><div class="panel-heading"><a href="{{ route('admin.printing-designs.create') }}" class="btn btn-add"><i class="fa fa-plus"></i> Add Printing Design</a></div><div class="panel-body"><form method="GET" class="form-inline"><input name="search" value="{{ request('search') }}" class="form-control input-sm" placeholder="Search name or code"><select name="status" class="form-control input-sm"><option value="">All statuses</option>@foreach($statusOptions as $value => $label)<option value="{{ $value }}" @selected(request('status') === $value)>{{ $label }}</option>@endforeach</select><button class="btn btn-primary btn-sm">Search</button> <a href="{{ route('admin.printing-designs.index') }}" class="btn btn-default btn-sm">Reset</a></form><div class="table-responsive"><table class="table table-bordered table-striped"><thead><tr><th>Design Name</th><th>Design Code / Number</th><th>Description</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($designs as $design)<tr><td>{{ $design->design_name }}</td><td>{{ $design->design_code ?: '—' }}</td><td>{{ IlluminateSupportStr::limit($design->description, 100) ?: '—' }}</td><td>{{ $design->status->value }}</td><td><a class="btn btn-xs btn-info" href="{{ route('admin.printing-designs.edit', enc($design->getKey())) }}">Edit</a> @if($design->status->value === 'Active')<form class="inline-form" method="POST" action="{{ route('admin.printing-designs.deactivate', enc($design->getKey())) }}">@csrf @method('PATCH')<button class="btn btn-xs btn-warning">Deactivate</button></form>@else<form class="inline-form" method="POST" action="{{ route('admin.printing-designs.activate', enc($design->getKey())) }}">@csrf @method('PATCH')<button class="btn btn-xs btn-success">Activate</button></form>@endif</td></tr>@empty<tr><td colspan="5">No Printing Designs found.</td></tr>@endforelse</tbody></table></div>{{ $designs->links('vendor.pagination.bootstrap-4') }}</div></div></section></div>@include('admin.common.footer')</div>@include('admin.common.formfooterscript')</body></html>
