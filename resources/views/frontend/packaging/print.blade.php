<!doctype html><html lang="en"><head><meta charset="utf-8"><title>Packaging Slip PKG-{{ $packagingOrder->id }}</title><style>body{font:12px Arial;color:#000}table{width:100%;border-collapse:collapse;margin:8px 0}td,th{border:1px solid #000;padding:5px;vertical-align:top}th{text-align:center;background:#eee}.no-border td{border:0}.right{text-align:right}.center{text-align:center}@media print{.no-print{display:none}}</style></head><body><div class="no-print"><button onclick="window.print()">Print Packaging Slip</button></div><table class="no-border"><tr><td style="width:20%">@if(!empty($company->logo))<img src="{{ asset($company->logo) }}" style="max-height:70px">@endif</td><td class="center"><h2 style="margin:0">{{ $company->name }}</h2><div>{{ $company->address ?? '' }}</div><h3>PACKAGING SLIP</h3></td><td class="right"><strong>PKG-{{ $packagingOrder->id }}</strong><br>Created: {{ optional($packagingOrder->created_at)->format('d-m-Y') ?: '-' }}<br>Status: {{ ucfirst($packagingOrder->packaging_status) }}</td></tr></table><table><tr><td width="50%"><strong>Customer:</strong> {{ $packagingOrder->customer->name ?? '-' }}<br><strong>Mode:</strong> {{ ucfirst($packagingOrder->packaging_mode) }}</td><td><strong>Parcels / Lots / Rolls:</strong> {{ $packagingOrder->parcel_count ?: '-' }} / {{ $packagingOrder->lot_count }} / {{ $packagingOrder->roll_count }}<br><strong>Allocated / Packed / Dispatched:</strong> {{ number_format((float) $packagingOrder->allocated_quantity, 2) }} / {{ number_format((float) $packagingOrder->packed_quantity, 2) }} / {{ number_format((float) $packagingOrder->dispatched_quantity, 2) }}</td></tr></table><table><thead><tr><th>Sale Order</th><th>Item / Detail</th><th>Packaging</th><th>Lot</th><th>Roll / Taka</th><th>Allocated</th><th>Packed</th></tr></thead><tbody>@foreach($packagingOrder->items as $item)@foreach($item->rollAllocations as $allocation)<tr><td>{{ $item->saleOrderItem?->saleOrder?->sale_order_number ?: '-' }}</td><td><strong>{{ $item->item_name ?: '-' }}</strong><br>{{ $item->grey_quality ?: '-' }} | {{ $item->dyeing_color ?: '-' }} | {{ $item->coating_type ?: '-' }}<br>{{ $item->final_dispatch_width ?: '-' }} / {{ $item->tube_width ?: '-' }}</td><td>{{ $item->packagingType?->name ?: '-' }}</td><td>{{ $allocation->dyeing_lot_number ?: '-' }}</td><td>{{ $allocation->packet_number ?: 'ROL-'.$allocation->warehouse_item_stock_id }}<br>{{ $allocation->insp_taka_number ?: '-' }}</td><td class="right">{{ number_format((float) $allocation->allocated_quantity, 2) }}</td><td class="right">{{ number_format((float) $allocation->packed_quantity, 2) }}</td></tr>@endforeach@endforeach</tbody></table><table><thead><tr><th>Lot-wise Total</th><th>Roll / Taka Count</th><th>Allocated</th><th>Packed</th></tr></thead><tbody>@foreach($lotTotals as $lot => $total)<tr><td>{{ $lot }}</td><td class="right">{{ $total['roll_count'] }}</td><td class="right">{{ number_format($total['allocated_quantity'], 2) }}</td><td class="right">{{ number_format($total['packed_quantity'], 2) }}</td></tr>@endforeach</tbody></table><p><strong>Remarks:</strong> {{ $packagingOrder->remarks ?: '-' }}</p><p>This is a computer-generated packaging slip.</p></body></html>
