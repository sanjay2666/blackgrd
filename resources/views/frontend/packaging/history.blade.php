<!DOCTYPE html>
<html lang="en"><head>@include('frontend.common.head', ['pageTitle' => 'Packaged Orders | Loomexa'])</head>
<body class="hold-transition sidebar-mini"><div class="wrapper">@include('frontend.common.header')<div class="content-wrapper"><section class="content"><div class="row"><div class="col-sm-12">
{!! display_message('message') !!}<div class="panel panel-bd lobidrag"><div class="panel-heading"><div class="btn-group"><a href="{{ route('packaging.show-packaged-orders') }}"><h4>Packaged Orders / History</h4></a></div><a class="btn btn-success btn-sm pull-right" href="{{ route('packaging.show-available-orders') }}"><i class="fa fa-plus"></i> Packaging Available</a></div><div class="panel-body">
<form method="get" class="row" style="margin-bottom:12px"><div class="col-sm-2"><input class="form-control input-sm" name="packaging_number" value="{{ request('packaging_number') }}" placeholder="PKG number"></div><div class="col-sm-2"><input class="form-control input-sm" name="customer_name" id="packaging-customer-search" value="{{ request('customer_name') }}" placeholder="Customer"><input type="hidden" name="customer_id" id="packaging-customer-id" value="{{ request('customer_id') }}"></div><div class="col-sm-2"><input class="form-control input-sm" name="sale_order" id="packaging-sale-order-search" value="{{ request('sale_order') }}" placeholder="Sale Order"><input type="hidden" name="sale_order_id" id="packaging-sale-order-id" value="{{ request('sale_order_id') }}"></div><div class="col-sm-1"><input class="form-control input-sm" name="item" id="packaging-item-search" value="{{ request('item') }}" placeholder="Item"><input type="hidden" name="item_id" id="packaging-item-id" value="{{ request('item_id') }}"></div><div class="col-sm-1"><input class="form-control input-sm" name="lot" id="packaging-lot-search" value="{{ request('lot') }}" placeholder="Lot"></div><div class="col-sm-1"><select class="form-control input-sm" name="packaging_status"><option value="">Status</option>@foreach(['draft','accepted','packed','dispatched','cancelled'] as $status)<option value="{{ $status }}" @selected(request('packaging_status') === $status)>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="col-sm-1"><input class="form-control input-sm" type="date" name="from_date" value="{{ request('from_date') }}"></div><div class="col-sm-1"><input class="form-control input-sm" type="date" name="to_date" value="{{ request('to_date') }}"></div><div class="col-sm-1"><button class="btn btn-default btn-sm">Filter</button></div></form>
<div class="table-responsive"><table class="table table-bordered table-striped table-hover"><thead><tr class="info"><th>Packaging</th><th>Customer</th><th>Sale Order(s)</th><th>Item(s)</th><th>Packaging Type</th><th>Lots / Rolls</th><th>Allocated</th><th>Packed</th><th>Dispatched</th><th>Remaining</th><th>Status / Created</th><th>Actions</th></tr></thead><tbody>@forelse($packagingOrders as $order)<tr><td>PKG-{{ $order->id }}</td><td>{{ $order->customer->name ?? '-' }}</td><td>{{ $order->sale_order_numbers->join(', ') ?: '-' }}</td><td>{{ $order->item_names->join(', ') ?: '-' }}</td><td>{{ $order->packaging_type_names->join(', ') ?: '-' }}</td><td>{{ $order->lot_count }} / {{ $order->roll_count }}</td><td>{{ number_format((float) $order->allocated_quantity, 2) }}</td><td>{{ number_format((float) $order->packed_quantity, 2) }}</td><td>{{ number_format((float) $order->dispatched_quantity, 2) }}</td><td>{{ number_format((float) $order->remaining_quantity, 2) }}</td><td><span class="label label-{{ $order->packaging_status === 'cancelled' ? 'danger' : ($order->packaging_status === 'packed' || $order->packaging_status === 'dispatched' ? 'success' : 'warning') }}">{{ ucfirst($order->packaging_status) }}</span><br><small>{{ optional($order->created_at)->format('d-m-Y') ?: '-' }} / {{ $order->created_by ?: '-' }}</small></td><td><a class="btn btn-primary btn-xs" href="{{ route('packaging.show-order-details', $order->id) }}">View</a> <a class="btn btn-default btn-xs" target="_blank" href="{{ route('packaging.print-packaging-slip', $order->id) }}">Print Slip</a>@if(in_array($order->packaging_status, ['accepted','packed'])) <a class="btn btn-info btn-xs" href="{{ route('packaging.show-order-details', $order->id) }}#pack-quantity">{{ $order->packaging_status === 'accepted' ? 'Pack Quantity' : 'Continue' }}</a>@endif @if($order->dispatchable_quantity > 0 && !in_array($order->packaging_status, ['cancelled'])) <a class="btn btn-success btn-xs" href="{{ route('sales-challans.create') }}">Sales Challan</a>@endif</td></tr>@empty<tr><td colspan="12" class="text-center">No Packaging Order found.</td></tr>@endforelse</tbody></table></div>{{ $packagingOrders->withQueryString()->links('vendor.pagination.bootstrap-4') }}</div></div>
</div></div></section></div>@include('frontend.common.footer')</div>@include('frontend.common.footerscript')@include('frontend.packaging.partials.filter-autocomplete')</body></html>
